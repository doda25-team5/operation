{{- if and .Values.istio.enabled .Values.monitoring.enabled -}}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Values.monitoring.prometheusRelease }}-prometheus-vs
spec:
  hosts:
  - {{ .Values.monitoring.hosts.prometheus }}
  gateways:
  - {{ .Values.istio.ingressGateway.name }}
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Values.monitoring.prometheusRelease }}-prometheus
        port:
          number: 9090
{{- end }}