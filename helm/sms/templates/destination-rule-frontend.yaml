apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ .Values.frontend.name }}-dr
spec:
  host: {{ .Values.frontend.service.name }}
  subsets:
    - name: stable
      labels:
        version: v1
    - name: canary
      labels:
        version: v2
