{{- if .Values.monitoring.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.monitoring.alerts.email.secretName }}  # ← From values
  namespace: default
type: Opaque
stringData:
  {{ .Values.monitoring.alerts.email.secretKey }}: {{ .Values.monitoring.alerts.email.password | quote }}
{{- end }}