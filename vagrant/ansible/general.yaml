- hosts: all
  become: yes
  tasks:
    # step 4
    - name: add SSH public keys to authorized_keys
      authorized_key:
        user: vagrant
        state: present
        key: "{{ lookup('file', item) }}"
      with_fileglob: 
        - "../keys/*.pub" 